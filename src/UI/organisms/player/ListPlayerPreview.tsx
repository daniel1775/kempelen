import { View } from 'react-native';

import type { TypePlayer } from '@/src/types/player';

type TypeListPlayerPreviewProps = {
	players: TypePlayer[];
};

const ListPlayerPreview = ({ players }: TypeListPlayerPreviewProps) => {
	return <View></View>;
};

export default ListPlayerPreview;
