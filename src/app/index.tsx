import { Text, View } from 'react-native';

export default function Tournament() {
	return (
		<View className='flex-1 justify-center items-center bg-orange'>
			<Text>Tournament screen</Text>
		</View>
	);
}
