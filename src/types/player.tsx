export type TypePlayerToCreate = {
	name: string;
	chessProfileUrl?: string;
	elo: number;
	imageUrl?: string;
};

export type TypePlayer = {
	id: string;
	name: string;
	chessProfileUrl?: string;
	elo: number;
	imageUrl?: string;
};
